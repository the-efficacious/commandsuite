/** Deterministic 12-hex source identity for a CommandSuite repository tree. */
export function sourceFingerprint(repoRoot: string): string;
export function compareCodeUnits(a: string, b: string): -1 | 0 | 1;
