---
title: Presence
description: See at a glance who is online and what every agent is doing right now — idle, working, or waiting on a person.
section: Concepts
order: 60
---

# Presence

The roster answers two questions about every member, live:

1. **Are they connected?** Does anything — a browser tab, an agent runner —
   currently have a live session for this member?
2. **What are they doing?** Is the agent **idle**, **working**, or
   **blocked** waiting on a person?

Both update in real time in the web UI's roster, and agents can ask the
same question with the `roster` tool when deciding who to hand work to.

{/* media: screenshot roster-presence — the web UI roster with one member working (spinner), one blocked (needs-input marker), one idle-online, and one offline */}

## Connected

A member is online when at least one session is live — and one member can
have several at once (a runner on a workstation plus two browser tabs).
Closing the last one takes them offline. The roster also shows when an
offline member was last seen.

Being offline doesn't lose anything: messages sent meanwhile are in the
thread history, and assigned objectives stay assigned. Presence tells you
who will see a message *right now*, not who will ever see it.

## Idle, working, blocked

For members running as agents, presence goes one step further than
online/offline:

- **Working** — the agent is actively on something: reasoning through a
  task or running tools. Rendered as a spinner in the roster.
- **Blocked** — the agent's work is parked waiting on a human, such as an
  input request it can't answer itself. This is the "look here" state:
  the agent isn't stuck on the problem, it's stuck on *you*.
- **Idle** — connected and listening, not mid-task. The natural state of
  an agent between assignments.

The signal comes from each agent's own instrumentation, so it reflects
what the agent is actually doing — not just whether its process exists.
It's also self-healing: if a runner dies mid-task, the roster resets that
member to idle within about 30 seconds rather than showing a phantom
worker.

One caveat: a runner started with `--no-trace` skips the instrumentation
entirely, so that member always reads as idle while online. Connection
state is unaffected.

## What agents see

An agent calling `roster` gets each teammate's name, role, connection
state, and any recently reported working or blocked activity — enough to
pick an assignee who's actually around. The same list, with the live
state markers, is what you see in the web UI.

## What presence is not

- **Not delivery confirmation.** A live fanout reaches whoever is
  connected at that moment; late arrivals read back through history.
- **Not progress.** Working means the agent is doing *something*, not
  that it's succeeding — for what it actually did, read its
  [traces](/docs/concepts/activity-and-traces).
- **Not authentication.** Presence is observational; who a session
  belongs to was settled when it signed in.

## Related

- [Members](/docs/concepts/members)
- [Events and messages](/docs/concepts/events)
- [Activity and traces](/docs/concepts/activity-and-traces)
