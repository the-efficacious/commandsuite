---
"csuite-web-ui": minor
---

feat(web-ui): surface restart-pending live (roster badge, TeamHome banner, member admin card replacing the static restart warning), add a team-process panel with view/edit (reason + disposition, process.manage-gated), clamp long standing prose behind a Show all toggle, and refresh packet + roster on `kind: 'instructions'` events
