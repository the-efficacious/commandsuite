---
'csuite-web-ui': patch
---

Keep id-bearing turns visibly unmatched when their exact inference record never arrives, instead of attaching a compatible neighbouring record by interval containment.
