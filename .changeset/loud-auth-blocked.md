---
'csuite-sdk': minor
'csuite-core': minor
'csuite-cli': minor
---

Notify long-running clients when bearer authentication is rejected, expose
token-aware blocked presence, and let runners retain capture while saved device
auth is replaced after re-enrolment.
